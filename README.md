# Iron Bear — waitlist site

Public waitlist landing for Iron Bear. Vite + React + TypeScript + Supabase.

```bash
cp .env.example .env.local       # fill in your Supabase URL + anon key
npm install
npm run dev
```

For everything else — design tokens, telemetry contract, full Supabase setup,
deploy steps, copy-tweak guide — see [`CLAUDE.md`](./CLAUDE.md).
